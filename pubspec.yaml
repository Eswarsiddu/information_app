name: information_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.18.0-271.4.beta <3.0.0'

dependencies:
  flutter:
    sdk: flutter
 
  http: ^0.13.5
  path_provider: ^2.0.11
  connectivity_plus: ^2.3.6+1
  path: ^1.8.2
  wakelock: ^0.6.2
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^2.0.0

flutter:  
  uses-material-design: true
  
  assets:
    - assets/
  
